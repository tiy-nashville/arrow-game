// Import the background image.

// How many

// Let there be balloons!!!

// Run something each "frame"
loop(function () {
    // Render the background image every iteration FIRST!

    // Update the state of the player.

    // Should the balloons move?

    // Where do the arrows go?

    // Should we make the baloons pop?

});
